apiVersion: v1
kind: Secret
metadata:
  name: anthropic-secret
type: Opaque
data:
  # base64 인코딩된 값은 배포 전 아래 명령으로 주입:
  # kubectl create secret generic anthropic-secret \
  #   --from-literal=ANTHROPIC_API_KEY="sk-ant-..." \
  #   --dry-run=client -o yaml | kubectl apply -f -
  ANTHROPIC_API_KEY: ""
