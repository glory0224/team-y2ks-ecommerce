# ============================================================
# Terraform이 aws configure를 읽어서 자동으로 채워주는 값들
# 직접 수정하지 말 것 — terraform apply 시 자동 주입됨
# ============================================================
accountId: ""
region: "ap-northeast-2"
clusterName: "y2ks-eks-cluster"
workerRoleArn: ""
karpenterNodeRoleName: ""

# ============================================================
# 앱 설정 — 필요 시 수정 가능
# ============================================================
queueName: "y2ks-queue"
kinesisStreamName: "y2ks-traffic-stream"
ddbTable: "y2ks-coupon-claims"
senderEmail: "wooseoyun@naver.com"
ticketCount: 100

images:
  frontend: ""  # terraform apply 시 자동 주입 — 직접 수정 금지
  worker: ""    # terraform apply 시 자동 주입 — 직접 수정 금지

worker:
  minReplicas: 1
  maxReplicas: 50
  shardCount: "1"
